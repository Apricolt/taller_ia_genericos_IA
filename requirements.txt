# Taller 1 - Algoritmos Geneticos
#
# Se declaran versiones MINIMAS y no versiones exactas. Una plataforma de
# despliegue no corre necesariamente la misma version de Python que el equipo
# de desarrollo, y un pin exacto sin rueda disponible para esa version hace
# fallar TODA la instalacion, dejando la aplicacion sin ninguna dependencia.

numpy>=1.26
pandas>=2.0
matplotlib>=3.7

# streamlit deliberadamente SIN version exacta: Streamlit Community Cloud
# instala la suya y un pin puede entrar en conflicto con ella.
streamlit>=1.30
